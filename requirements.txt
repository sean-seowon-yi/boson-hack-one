# -------------------------------
# PyTorch CPU wheels (MUST be first)
# -------------------------------
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.3.1
torchaudio==2.3.1
# torchvision==0.18.1   # uncomment only if truly needed

# -------------------------------
# Source separation / ASR
# -------------------------------
demucs>=4.0.0
git+https://github.com/m-bain/whisperx.git   # WhisperX (needs numpy >=2.0.2,<2.1)

# -------------------------------
# Web UI / plumbing
# -------------------------------
gradio==5.49.1
loguru
python-dotenv
dotenv
requests

# -------------------------------
# Media / audio stack
# -------------------------------
# NumPy 2.x to satisfy WhisperX
numpy==2.0.2
# librosa with NumPy 2 compatibility
librosa==0.10.2.post1
scipy>=1.11
soundfile
audioread
ffmpeg-python
yt-dlp
moviepy
imageio
imageio-ffmpeg
audiostretchy
wget==3.2
pyarrow

# -------------------------------
# NLP / models
# -------------------------------
transformers==4.39.3
accelerate
diffusers==0.27.2

# -------------------------------
# TTS / ASR / supporting libs
# -------------------------------
edge-tts
aiohttp
funasr
modelscope
conformer==0.3.2
WeTextProcessing==1.0.3
HyperPyYAML==1.2.2
